services:
  frontend:
    image: binroute-frontend:latest
    container_name: binroute-frontend
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3737:3737"
    env_file:
      - .env
    environment:
      NODE_ENV: production
